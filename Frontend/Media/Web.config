<?xml version="1.0"?>
<configuration>
    <system.web>
      <authorization>
        <!-- Allow all users (including anonymous) to access media files. -->
        <allow users="*"/>
      </authorization>
    </system.web>
</configuration>
